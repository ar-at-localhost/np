pre-commit:
  commands:
    format-nix:
      run: nix fmt .
      glob: "*.nix"
      stage_fixed: true
    check-flake:
      run: nix flake check
      glob: "*.nix"
    lint-markdown:
      run: markdownlint --ignore result/ --fix .
      glob: "*.md"
      stage_fixed: true
